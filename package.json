{
  "name": "calle-heat-guardian",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build",
    "typecheck": "npm --prefix frontend run typecheck"
  }
}
